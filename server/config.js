module.exports = {
  pokeStart: 1, // Sets starting pokedex id
  pokeEnd: 15, // Number of pokemon to grab from the api
  randomTypes: ['grass', 'fire', 'water'], // array of types the random function grabs from /getRandom
  queryQty: 3 // Number of Pokemon returned for each type, per query
};
